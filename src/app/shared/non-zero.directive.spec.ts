import { NonZeroDirective } from './non-zero.directive';

describe('NonZeroDirective', () => {
  it('should create an instance', () => {
    const directive = new NonZeroDirective();
    expect(directive).toBeTruthy();
  });
});
